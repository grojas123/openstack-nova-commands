# openstack-nova-commands
Get ta .csv file of volumes per instances with OpenStack nova command
I had a issues when I try to match all the instances in OpenStack wiht the volumes in cinder .
I created this script to get a .csv file in a shell script .
